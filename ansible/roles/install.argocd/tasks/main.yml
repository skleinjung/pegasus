- name: Install argocd
  delegate_to: toolbox
  ansible.builtin.shell: |
    arkade install argocd && \
    mkdir -p /etc/ansible-locks && \
    touch /etc/ansible-locks/argocd
  args:
    creates: "/etc/ansible-locks/argocd"